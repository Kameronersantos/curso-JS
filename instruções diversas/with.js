/* a intrução  with pode ser usada par formulario */


with(o) x = 1;
console,console.log(o);

with(document.forms[0]) {
    name.value = "";
    address.value = "";
    email.value = "";
}
/* with funciona encadeamento de escopo para objetos */